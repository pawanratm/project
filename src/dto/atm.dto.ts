export interface AtmDTO {
  id: number;
  name: string;
  value: number;
  amount: number;
}

export interface CustomerDTO {
  id: number;
  name: string;
  balance: number;
  accountNumber: string;
}
